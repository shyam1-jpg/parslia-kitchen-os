# PC Guard folder (older desktop experiments)

The product is now the **web app**:

→ open **`/guard/`** (see `../guard/README.md`)

You do **not** need `.bat` files, Python, or a desktop window.

The scripts in this folder are legacy/optional only.
