{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "parslia-ai-builder: build",
      "type": "shell",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}/parslia-ai-builder"
      },
      "problemMatcher": ["$tsc"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
